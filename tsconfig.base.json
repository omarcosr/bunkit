{
  // Base tsconfig for apps using @omarcos/bunkit's JSX runtime.
  //
  //   {
  //     "extends": "@omarcos/bunkit/tsconfig",
  //     "include": ["src"]
  //   }
  //
  // Without jsxImportSource: "@omarcos/bunkit", the editor resolves JSX through
  // React's runtime, which types every element as ReactElement — so
  // `const log = <TextArea …/>` loses its control type.
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["ESNext"],
    "module": "preserve",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "jsxImportSource": "@omarcos/bunkit",
    "strict": true,
    "skipLibCheck": false
  }
}
